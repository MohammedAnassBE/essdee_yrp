/**
 * Item Production Detail — per-DocType field config consumed by DocDetail.vue.
 *
 * essdee's IPD carries a large customization layer (packing / set item /
 * stiching / cutting / fabric tabs) as Custom Fields; those render meta-driven
 * in the generic editor (Table fields as editable child tables, JSON blobs
 * hidden). No per-field overrides needed yet — this module exists so future
 * IPD-specific labels/handlers have an obvious home.
 */
export default {}
